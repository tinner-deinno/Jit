---
description: v26.5.16 L-CMD | Daily standup check — pending tasks, appointments, recent progress, schedule. Use when user says "standup", "morning check", "what's pending", or at the start of a work day. Do NOT trigger for mid-session status (use /recap --now), session orientation (use /recap), or retrospectives (use /rrr).
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
  - WebFetch
---

# /standup

Execute the `standup` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "standup"` instead of reading the file manually.

**Otherwise**: Read the skill file at `C:\Users\admin\Jit\.claude\skills/standup/SKILL.md` and follow ALL instructions in it.

---
*arra-oracle-skills-cli v26.5.16*
