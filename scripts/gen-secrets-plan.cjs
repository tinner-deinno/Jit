'use strict';
const fs = require('fs');

const ENV_NAMES = 'OLLAMA_TOKEN, COMMANDCODE_API_KEY (user_ prefix + ~70 base62 chars), THAILLM_TOKEN (32 hex), DISCORD_TOKEN (Discord bot format MT...), CODEX_API_KEY (RS256 JWT eyJ...), OPENAI_API_KEY (sk-...), GITHUB_COPILOT_TOKEN, MARIADB_PASSWORD, JWT_SECRET, OLLAMA_API_KEY';
const STACK = 'Monorepo: innomcp (Next.js 14 app router on :3000 + Express/Node22 backend innomcp-node on :3011, MariaDB :3308, Redis, pnpm workspaces, docker-compose, Windows dev) and Jit (bash/node oracle scripts, .github/agents/*.md, brain dir). Dev on Windows + Git Bash; prod via docker-compose. CI: GitHub Actions, Node22+pnpm11.';

const mk = (id, role, model, out, task) => ({ id, role, model, status: 'pending', output: 'ψ\\outbox\\cc-team\\' + out, task });

const plan = {
  plan_id: 'CC-TEAM-2026-06-13-SECRETS-OVERHAUL',
  planned_by: 'jit (Fable 5)', sa_reviewer: 'jit', provider: 'commandcode',
  created_at: new Date().toISOString(),
  context: 'Secrets Overhaul per docs/spec20260613.md. Discovery done: both Jit + innomcp are git-clean (no hardcoded secrets in tracked files or history); real secrets live only in gitignored .env. Gaps are 100% prevention/automation tooling. Build installable artifacts for BOTH repos. ' + STACK + ' Env var names in use: ' + ENV_NAMES,
  groups: [{
    id: 'SEC',
    mission: 'Secrets overhaul tooling + docs (gitleaks, hooks, CI, sanitizer, playbook, README, hardened compose)',
    members: [
      mk('S1', 'dev', 'deepseek/deepseek-v4-pro', 'SEC-S1-gitleaks.md',
        'Output ONLY raw TOML (no markdown fences, no commentary). Write a complete .gitleaks.toml (gitleaks v8 config) for this project. Requirements: (1) title + extend default rules using [extend] with useDefault=true. (2) custom [[rules]] for these project-specific secret shapes, each with id, description, regex, keywords: a COMMANDCODE key (literal prefix user_ followed by 60-90 chars of [A-Za-z0-9]); a 32-char lowercase-hex generic API token keyed on the var names OLLAMA_TOKEN or THAILLM_TOKEN (regex matches NAME = value); a Discord bot token (segments separated by dots, first segment 24+ chars); an RS256 JWT regex (eyJ[A-Za-z0-9_-]+ dot eyJ...) keyed to CODEX_API_KEY; a generic OpenAI sk- key. (3) [allowlist] with paths regex ignoring: .env.example files, anything under docs/, .md placeholder docs, pnpm-lock.yaml, node_modules; and a regexes entry ignoring literal placeholders (YOUR_, REDACTED, example, change-me, and the ${ shell-var marker). Keep allowlist tight enough that a real .env force-added would still be caught. Add header # comments explaining each custom rule. Production-ready.'),
      mk('S2', 'dev', 'Qwen/Qwen3.7-Max', 'SEC-S2-precommit.md',
        'Output ONLY raw POSIX sh (no markdown fences), starting with the shebang line for sh. Write a git pre-commit hook script (also usable as pre-push) named secrets-guard.sh for a Windows+GitBash and Linux project. Behavior: (1) Block the commit (exit 1, clear message) if any staged file path matches .env or .env.* except files ending in .env.example; list offending paths. (2) If gitleaks is on PATH, run gitleaks protect --staged --redact with --config .gitleaks.toml; fail commit on non-zero exit. (3) If gitleaks is NOT installed, fall back to scanning staged content via git diff --cached for high-entropy secret patterns: sk- keys, ghp_ tokens, user_ + 60 chars, JWT eyJ...eyJ, AKIA + 16, BEGIN PRIVATE KEY, and 32-char hex assigned to a *TOKEN= name; print a masked match (first 6 chars then ...) plus the file; exit 1 on any hit. (4) Always print how to intentionally bypass (the --no-verify flag) AND warn that bypass is discouraged. (5) Echo a one-line OK when clean. Pure POSIX sh, no bashisms, double-quote all variables. Under 110 lines.'),
      mk('S3', 'dev', 'moonshotai/Kimi-K2.6', 'SEC-S3-ci.md',
        'Output ONLY raw YAML (no markdown fences, no prose). Write .github/workflows/secret-scan.yml, a GitHub Actions workflow. Requirements: name is secret-scan. Triggers: on push to branch main, and on pull_request. Top-level permissions: contents read, id-token write (for OIDC), pull-requests write. Job gitleaks: runs-on ubuntu-latest; steps: actions/checkout@v4 with fetch-depth 0 for full history, then gitleaks/gitleaks-action@v2 with env GITLEAKS_CONFIG set to .gitleaks.toml and a commented GITLEAKS_LICENSE placeholder. Job verify-artifacts: runs-on ubuntu-latest, a bash step that greps the PR/commit diff for referenced file paths (in backticks or quotes, ending .ts/.js/.md) and prints a non-failing warning if a referenced path does not exist on disk (guards against hallucinated-artifact references from bulk generation). Job block-env-files: a bash step that fails if git ls-files lists a real .env file (not .env.example). Add concise hash comments. Valid YAML, correct two-space indentation.'),
      mk('S4', 'dev', 'moonshotai/Kimi-K2.6', 'SEC-S4-sanitize.md',
        'Output ONLY raw JavaScript (no markdown fences), starting with the node shebang line then a block comment. Write scripts/sanitize-log.js, a zero-dependency Node.js CommonJS utility that masks secrets in text. Use module.exports to export a function sanitize(text) that replaces these patterns keeping only the first 4 chars then the literal ...REDACTED: sk- keys (20+ chars), ghp_/gho_/github_pat_ tokens, user_ + 60 chars, RS256 JWT (eyJ...dot eyJ...dot rest), AKIA + 16, Discord MT-style three-segment token, the word Bearer followed by a token, and any KEY=/TOKEN=/SECRET=/PASSWORD= assignment (mask only the value, keep the name), plus 32+ char hex or base64 high-entropy runs. Also export maskValue(v). When run directly (require.main === module): read all of stdin and write sanitize(stdin) to stdout so it works as a pipe filter. Use strict mode, comment well, and make it idempotent (re-running on already-masked text must not double-mask). Under 130 lines.'),
      mk('S5', 'writer', 'deepseek/deepseek-v4-flash', 'SEC-S5-playbook.md',
        'Output ONLY raw markdown (no surrounding fences). Write a Thai-language secret-leak incident response playbook (English for technical terms and commands). Providers/tokens: OLLAMA_TOKEN (MDES Ollama at ollama.mdes-innova.online), COMMANDCODE_API_KEY (commandcode.ai), THAILLM_TOKEN, DISCORD_TOKEN, CODEX_API_KEY (OpenClaude JWT), OPENAI_API_KEY, GitHub PAT. Sections in Thai: a 60-second triage section; a Revoke/Rotate table per provider with columns provider, how to revoke old, how to issue new, where to set the new value (.env or Doppler), dashboard link; a post-rotate section (purge git history with git filter-repo if it was committed, invalidate caches, notify team via Discord/LINE); a tabletop drill section with 3 scenarios and a checklist; an incident log template. Honor the Nothing-is-Deleted principle: always keep a log of rotations. Under 130 lines.'),
      mk('S6', 'writer', 'Qwen/Qwen3.7-Max', 'SEC-S6-readme.md',
        'Output ONLY raw markdown (no surrounding fences). Write the canonical Thai-language (English technical terms) secrets management guide for this monorepo. Sections in Thai: current status as a 14-row checklist table with columns number, task, status (done / partial / not-done), note — fill it truthfully: repos are git-clean, .env is gitignored correctly, but there is not yet gitleaks/pre-commit/CI-scan/vault/rotation/OIDC; a section on tools installed this round (gitleaks config, pre-commit hook secrets-guard.sh, CI secret-scan.yml, sanitize-log.js) with install instructions (git config core.hooksPath, installing gitleaks on Windows via scoop or choco); a policy section (least-privilege per token, recommended short-TTL rotation, never edit .gitignore to track .env); an OIDC-for-GitHub-Actions section (stop storing long-lived tokens in Actions secrets, request ephemeral tokens from the cloud provider, with a permissions block example); an RBAC table (who can access which token); a Vault recommendation (Doppler suits this scale, with reasons). Under 140 lines.'),
      mk('S7', 'dev', 'deepseek/deepseek-v4-pro', 'SEC-S7-compose-harden.md',
        'Output ONLY raw markdown with exactly two fenced code blocks, each preceded by a one-line note. This is a security hardening patch for the innomcp docker-compose.yml which currently uses weak dev-default passwords like the MARIADB_ROOT_PASSWORD default innomcp_root and MARIADB_PASSWORD default innomcp_pass (these defaults ship to anyone who runs compose without a .env). Produce: (1) a one-line note then a yaml code block showing the corrected mariadb and api service environment sections that REMOVE the insecure default fallbacks and instead use the compose required-variable syntax that errors if unset (the form with ?error message) for all of MARIADB_ROOT_PASSWORD, MARIADB_PASSWORD, DB_PASSWORD, JWT_SECRET; keep non-secret defaults like DB_NAME and ports unchanged. (2) a one-line note then a bash code block with the exact apply steps plus a line to append the needed vars as empty placeholders to .env.example. Keep it surgical: only the changed keys with 2-3 lines of surrounding context per block so placement is unambiguous.')
    ]
  }]
};

fs.writeFileSync(require('path').join(__dirname, '..', '.planning', 'cc-team-plan.json'), JSON.stringify(plan, null, 2));
console.log('SECRETS plan: 7 tasks written');
